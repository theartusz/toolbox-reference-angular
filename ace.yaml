# App Configuration
name: toolbox-reference-angular

# Contact Information
contact:
  responsible: DevOps
  slack_channel: '#reference-apps'

# Helm chart configuration
helm:
  chart: ace/web
  version: ^4.0.0
  cluster: kubernetes.maze-test
  registry: evrydevopsprod.azurecr.io
  repo: https://evry-ace.github.io/helm-charts/
  repoName: ace

  values:
    liveness:
      port: 80
    readiness:
      port: 80
    service:
      targetPort: 80
    metrics:
      enabled: false

environments:
  dev:
    cluster: kubernetes.maze-test
    namespace: toolbox-dev
    values:
      ingress:
        host: toolbox-reference-angular.dev.maze.evry.site
  test:
    cluster: kubernetes.maze-test
    namespace: toolbox-test
    overrides:
      ingress:
        host: toolbox-reference-angular.test.maze.evry.site
  prod:
    cluster: kubernetes.maze-prod
    namespace: toolbox-prod
    overrides:
      ingress:
        host: toolbox-reference-angular.prod.maze.evry.site




